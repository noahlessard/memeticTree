package main

templ submission(selectedString string, selectedTags string, selectedType string, status bool) {

<!DOCTYPE html>
<html>

@style()

<body>
    <div class="center">
    @homebutton()    
    
    @links()
    
    <h3>Submissions:</h3>
    <form method="POST" enctype="multipart/form-data">

    <p>Thank you for taking the time to submit a meme to this archive. You don't need an account to submit, but if you would like to track your contributions or have them count towards becoming a moderator, consider creating an account first.</p>

    <p> You can reference an existing node in the archive by putting its name here: </p>
    <input type="text" name="nameinput" value={selectedString}>
    <select name="searchType">
    	<option value="none" selected?={ selectedType == "none" }></option>
	    <option value="parent" selected?={ selectedType == "parent" }>Link to the existing node as a new parent</option>
		<option value="child" selected?={ selectedType == "child" }>Link to the existing node as a new child</option>
	</select>

    <p> Please consider tagging your image, so that we can better collate it to existing memes. Tags must be comma separated. Tags should be adjectives / categories that aren't memes themselves. Good tag examples include: "notext" , "video game", "twitter", "programming." </p>
    <input type="text" name="tagsinput" value={selectedTags}>

    <p>We currently accept jpg/jpeg, png, and webp images. Please limit file size to &lt; 2 MB</p>

        <input type="file" id="image" name="image" accept="image/jpeg,image/png,image/webp" required />
        <button type="submit">Upload all info</button>
    </form>

    <script>
        document.getElementById("image").form.onsubmit = function () {
            var f = document.getElementById("image").files[0];
            if (f && f.size > 2097152) {
                alert("File too large, must be under 2MB");
                return false;
            }
            return true;
        };
    </script>

    if status == true{
        <p>Successfully submitted!</p>
    }
 

    </div>

</body>

</html>

}